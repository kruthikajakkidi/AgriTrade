{
  "name": "agritrade-monorepo",
  "version": "1.0.0",
  "description": "AgriTrade - Farm Produce Procurement & Supply Chain Management Platform",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm start",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["agritech", "supply-chain", "mern", "procurement", "agriculture"],
  "author": "AgriTrade Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
